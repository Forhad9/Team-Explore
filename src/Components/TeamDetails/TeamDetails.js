import React from 'react';
import { useParams } from 'react-router';

const TeamDetails = () => {
    
    return (
        <div>
            <h1>this is team information {teamId}</h1>
        </div>
    );
};

export default TeamDetails;